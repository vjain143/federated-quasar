meta {
  name: Current User
  type: http
  seq: 2
}

get {
  url: {{base_url}}/v1/users/loggedInUser
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{jwt_token}}
}
