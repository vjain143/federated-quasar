meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/users/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "email": "{{om_email}}",
    "password": "{{om_password_b64}}"
  }
}
