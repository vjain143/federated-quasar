meta {
  name: List Classifications
  type: http
  seq: 1
}

get {
  url: {{base_url}}/v1/classifications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
