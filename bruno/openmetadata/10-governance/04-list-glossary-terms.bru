meta {
  name: List Glossary Terms
  type: http
  seq: 4
}

get {
  url: {{base_url}}/v1/glossaryTerms
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
