meta {
  name: List Domains
  type: http
  seq: 6
}

get {
  url: {{base_url}}/v1/domains
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
