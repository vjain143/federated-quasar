meta {
  name: List Dashboards
  type: http
  seq: 3
}

get {
  url: {{base_url}}/v1/dashboards
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
