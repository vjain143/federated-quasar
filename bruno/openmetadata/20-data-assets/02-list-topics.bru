meta {
  name: List Topics
  type: http
  seq: 2
}

get {
  url: {{base_url}}/v1/topics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
