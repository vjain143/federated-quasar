meta {
  name: List Tables
  type: http
  seq: 1
}

get {
  url: {{base_url}}/v1/tables
  body: none
  auth: bearer
}

params:query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{jwt_token}}
}
