meta {
  name: List ML Model Services
  type: http
  seq: 10
}

get {
  url: {{base_url}}/v1/services/mlmodelServices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
