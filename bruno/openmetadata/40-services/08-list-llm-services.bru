meta {
  name: List LLM Services
  type: http
  seq: 8
}

get {
  url: {{base_url}}/v1/services/llmServices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
