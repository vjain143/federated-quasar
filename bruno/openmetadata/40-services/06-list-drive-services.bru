meta {
  name: List Drive Services
  type: http
  seq: 6
}

get {
  url: {{base_url}}/v1/services/driveServices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
