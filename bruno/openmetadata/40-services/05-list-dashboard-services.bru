meta {
  name: List Dashboard Services
  type: http
  seq: 5
}

get {
  url: {{base_url}}/v1/services/dashboardServices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
