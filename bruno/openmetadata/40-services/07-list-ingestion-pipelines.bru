meta {
  name: List Ingestion Pipelines
  type: http
  seq: 7
}

get {
  url: {{base_url}}/v1/services/ingestionPipelines
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  limit: {{limit}}
  include: {{include}}
  fields: {{fields}}
}
