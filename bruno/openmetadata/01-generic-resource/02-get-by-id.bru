meta {
  name: Get Resource By ID
  type: http
  seq: 2
}

get {
  url: {{base_url}}{{resource_path}}/{{entity_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

headers {
  Accept: application/json
}

query {
  fields: {{fields}}
  include: {{include}}
}
