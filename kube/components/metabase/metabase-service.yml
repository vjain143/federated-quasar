apiVersion: v1
kind: Service
metadata:
  name: metabase
  labels:
    app: metabase
spec:
  type: NodePort
  ports:
    - name: https
      port: 3000
      nodePort: 30030
      targetPort: 3000
      protocol: TCP
  selector:
    app: metabase
