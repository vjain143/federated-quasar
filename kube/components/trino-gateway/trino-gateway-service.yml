apiVersion: v1
kind: Service
metadata:
  name: trino-gateway
spec:
  selector:
    app: trino-gateway
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30082
