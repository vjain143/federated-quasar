apiVersion: v1
kind: Service
metadata:
  name: airflow
  labels:
    app: airflow
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30880
      targetPort: 8080
  selector:
    app: airflow-web
