apiVersion: v1
kind: Service
metadata:
  name: fq-kestra
spec:
  selector:
    app: fq-kestra
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30882
  type: NodePort
