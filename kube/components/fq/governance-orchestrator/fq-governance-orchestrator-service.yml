apiVersion: v1
kind: Service
metadata:
  name: fq-governance-orchestrator
spec:
  selector:
    app: fq-governance-orchestrator
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
