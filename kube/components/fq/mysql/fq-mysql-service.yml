apiVersion: v1
kind: Service
metadata:
  name: fq-mysql
  labels:
    app: fq-mysql
spec:
  type: NodePort
  ports:
  - port: 3306
    nodePort: 30336
    targetPort: 3306
    protocol: TCP
  selector:
    app: fq-mysql
