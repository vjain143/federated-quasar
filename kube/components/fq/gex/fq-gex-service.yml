apiVersion: v1
kind: Service
metadata:
  name: fq-gex
spec:
  selector:
    app: fq-gex
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30881
  type: NodePort
