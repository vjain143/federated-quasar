connector.name=http

# Keep metadata relation DDL usable without external metastore in local Docker runs.
metadata.supported_relations=table,view
metadata.table.storage.mode=in_memory
metadata.view.storage.mode=in_memory
