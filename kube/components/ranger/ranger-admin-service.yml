apiVersion: v1
kind: Service
metadata:
  name: ranger-admin
  namespace: ranger
spec:
  selector:
    app: ranger-admin
  ports:
    - protocol: TCP
      port: 6080
      targetPort: 6080
  type: LoadBalancer # or NodePort depending on your environment
