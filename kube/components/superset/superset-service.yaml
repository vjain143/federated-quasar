apiVersion: v1
kind: Service
metadata:
  name: superset
  labels:
    app: superset
spec:
  type: NodePort
  ports:
    - name: http
      port: 8088
      nodePort: 30888
      targetPort: 8088
      protocol: TCP
  selector:
    app: superset