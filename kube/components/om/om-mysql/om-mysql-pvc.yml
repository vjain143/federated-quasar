apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: om-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Should match the storage capacity in the PV